DATABASE_HOST=
DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_PORT=

SECRET_KEY=
EXPIRY_TIME="1h"

# Stateless TOTP secret used to derive password-reset codes (never stored in DB).
# Optional — falls back to a built-in default if unset; set an explicit value in production.
TOTP_SERVER_SECRET_KEY=

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SENDER_EMAIL=
FRONTEND_URL=http://localhost:5173